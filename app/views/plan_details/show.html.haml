= link_to 'back to reading plan', plan_path(@plan_detail.plan)

%h2
  = render :partial => 'plan_detail_header', :locals => { plan_detail: @plan_detail }

= form_tag dashboard_comment_path do
  = text_area_tag :comment
  = hidden_field_tag :id, @plan_detail.id
  = button_tag :submit

= render(@comments) || 'No comments'
