%h1 Comments
%h2
  = render :partial => 'plan_detail_header', :locals => { plan_detail: @plan_detail }
  &nbsp;
  &nbsp;
  &nbsp;
  = link_to 'plan index', plan_path(@plan_detail.plan)

= render 'comments/form'

#emoji_list(style="margin:20px 0 20px 0;")
  - @emoji_list.each do |emoji|
    = EmojiHelper.linkify(emoji)

= render(@comments, total: @comments.count) || 'No comments'
